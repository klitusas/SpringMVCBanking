<!DOCTYPE HTML>
<html xmlns:th="http://www.thymeleaf.org">
<head>
	<title>Developer</title>
	<meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
	<link rel="stylesheet" href="/bootstrap.min.css" media="screen"/>
</head>
<body>
	<div class="container">
      <div class="page-header" id="banner">
        <div class="row">
          <div class="col-lg-8 col-md-7 col-sm-6">
			<h1>Developer</h1>
			<table class="table table-striped table-hover">
				<thead>
					<tr>
						<th>Name</th>
						<th>Accounts</th>
						<th></th>
					</tr>
				</thead>
				<tr th:each="user : ${users}">
					<td th:text="${user.firstName + ' ' + user.lastName}"></td>
					<td>
						<span th:each="account, iterStat : ${user.accounts}">
							<span th:text="${account.label}"/><th:block th:if="${!iterStat.last}">,</th:block>
						</span>
					</td>
					<td>
						<span th:each="account, iterStat : ${user.accounts}">
							<span th:text="${account.label}"/><th:block th:if="${!iterStat.last}">,</th:block>
						</span>
					</td>
					<td>
						<a >view</a>
					</td>
				</tr>
			</table>
		 </div>
	</div>
</div>
</div>

<!-- 	Name: <b th:text="${accounts.label}" /> <b th:text="${account}" /><br/> -->
<!-- 	User:
		<span th:each="user : ${account.users}">
			<br/>&nbsp;&nbsp;<span th:text="${user.firstName}" /> - <span th:text="${user.lastName}" />
		</span> -->
<!-- 	<form th:action="@{/account/{id}/users(id=${account.id})}" method="post" enctype="multipart/form-data" >
		<select name="userId">
			<option th:each="user : ${users}" 
				th:value="${user.id}" 
				th:text="${user.firstName}">Skill</option>
		</select>
		<input type="submit" value="Add user"/>
	</form>  -->
</body>
</html>